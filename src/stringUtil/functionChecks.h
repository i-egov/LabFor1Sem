#ifndef SU_F_CHECK_INCLUDED
#define SU_F_CHECK_INCLUDED

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// Checking strings for letters
int is_chars(const char* str);

// ПChecking strings for digits
int is_numbers(const char* str);

#endif